# mechanical_jerk

Using GPT-2 finetuned on the IAC arguments dataset to automate finding and arguing with people on twitter. Replace API keys with your own. Contact me if you'd like the pretrained model.

Parsed the dataset into statement response pairs.
